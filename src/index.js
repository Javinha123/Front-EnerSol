import './index.css';
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(
  <React.StrictMode>
      <App /> {/* Renderize diretamente a página Main */}
  </React.StrictMode>,
  document.getElementById('root')
);